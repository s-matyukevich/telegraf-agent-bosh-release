---
name: broker-additional-config

templates:
  pre-start.erb: bin/pre-start

properties:
  all_hosts:
    description: all mysql proxy hosts in failover order
  datacenter:
    description: datacenter name
